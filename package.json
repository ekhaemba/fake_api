{
  "name": "faking_server",
  "version": "1.0.0",
  "description": "Used to fake Calendar and user data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"babel src --out-dir dest",
    "start":"node dest/client/index.js"
    },
  "private": true,
  "author": "ekhaemba",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2",
    "json-schema-faker": "^0.4.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.2.1",
    "babel-preset-es2015": "^6.22.0"
  }
}
